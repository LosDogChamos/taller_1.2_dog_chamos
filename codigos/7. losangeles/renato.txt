-- 1. Limpieza de tablas previas para evitar conflictos de llaves foráneas
SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS Detalle;
DROP TABLE IF EXISTS Comprobante;
DROP TABLE IF EXISTS Producto;
DROP TABLE IF EXISTS Cliente;
DROP TABLE IF EXISTS Categoria;
SET FOREIGN_KEY_CHECKS = 1;

-- 2. Creación de tablas (Cambiando IDENTITY por AUTO_INCREMENT)
CREATE TABLE Categoria (
    idCat INT AUTO_INCREMENT PRIMARY KEY,
    nombreCat VARCHAR(100) NOT NULL,
    descripcionCat VARCHAR(250) NULL
) ENGINE=InnoDB;

CREATE TABLE Producto (
    idP INT AUTO_INCREMENT PRIMARY KEY,
    nombreP VARCHAR(100) NOT NULL,
    descripcionP VARCHAR(250) NULL,
    precioReferencialP DECIMAL(10, 2) NOT NULL,
    idCat INT NOT NULL,
    CONSTRAINT FK_Producto_Categoria FOREIGN KEY (idCat) REFERENCES Categoria(idCat)
) ENGINE=InnoDB;

CREATE TABLE Cliente (
    idC INT AUTO_INCREMENT PRIMARY KEY,
    tipoDocumC VARCHAR(20) NOT NULL,
    nroDocumC VARCHAR(20) NOT NULL,
    nombresC VARCHAR(100) NOT NULL,
    paternoC VARCHAR(100) NOT NULL,
    maternoC VARCHAR(100) NOT NULL,
    celularC VARCHAR(9) NOT NULL
) ENGINE=InnoDB;

CREATE TABLE Comprobante (
    idComp INT AUTO_INCREMENT PRIMARY KEY,
    fechaHoraComp DATETIME NOT NULL,
    totalComp DECIMAL(10, 2) NOT NULL,
    idC INT NOT NULL,
    CONSTRAINT FK_Comprobante_Cliente FOREIGN KEY (idC) REFERENCES Cliente(idC)
) ENGINE=InnoDB;

CREATE TABLE Detalle (
    idDet INT AUTO_INCREMENT PRIMARY KEY,
    cantidadDet INT NOT NULL,
    precioRealUnitarioDet DECIMAL(10, 2) NOT NULL,
    subtotalDet DECIMAL(10, 2) NOT NULL,
    idComp INT NOT NULL,
    idP INT NOT NULL,
    CONSTRAINT FK_Detalle_Comprobante FOREIGN KEY (idComp) REFERENCES Comprobante(idComp),
    CONSTRAINT FK_Detalle_Producto FOREIGN KEY (idP) REFERENCES Producto(idP)
) ENGINE=InnoDB;

-- 3. Inserción de Datos (Valores reales para prueba)
INSERT INTO Categoria (nombreCat, descripcionCat) VALUES
('Electrónica', 'Gadgets y equipos'), ('Ropa', 'Prendas de vestir');

INSERT INTO Producto (nombreP, descripcionP, precioReferencialP, idCat) VALUES
('Smartphone X', 'Teléfono última generación', 1500.00, 1),
('Camisa casual', 'Algodón hombre', 89.90, 2);

INSERT INTO Cliente (tipoDocumC, nroDocumC, nombresC, paternoC, maternoC, celularC) VALUES
('DNI', '12345678', 'Juan', 'Perez', 'Gomez', '987654321');

INSERT INTO Comprobante (fechaHoraComp, totalComp, idC) VALUES
('2026-03-01 10:30:00', 1589.90, 1);

INSERT INTO Detalle (cantidadDet, precioRealUnitarioDet, subtotalDet, idComp, idP) VALUES
(1, 1500.00, 1500.00, 1, 1),
(1, 89.90, 89.90, 1, 2);

-- 4. Verificación final para el reporte
SELECT 'CATEGORIA' AS Tabla, COUNT(*) AS Filas FROM Categoria
UNION ALL
SELECT 'PRODUCTO', COUNT(*) FROM Producto
UNION ALL
SELECT 'CLIENTE', COUNT(*) FROM Cliente;